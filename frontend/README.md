# ESDEMO frontend

Next.js App Router frontend for ESDEMO. Run commands from the repository root; the complete setup is documented in the root `README.md` and `docs/local-development.md`.

The `app` directory owns routing and layouts. Shared UI belongs in `components`, API access in `lib/api`, and future business code should be grouped by feature in `features`.
